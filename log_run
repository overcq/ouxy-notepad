#!/bin/sh
#==============================================================================
cd $( dirname $0 )/.build || exit 1
exec ocq term-args 'cd "$1" && ./notepad; sleep 1000' "$SHELL" "$PWD"
#==============================================================================
